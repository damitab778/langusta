export type { Translations, UILang } from './types';
export { uiLanguages } from './types';

import en from './en';
import es from './es';
import pl from './pl';

export const translations = { en, es, pl };
